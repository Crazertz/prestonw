package NBHgames;

import NBHgames.point;

public class Ninja 
{
	String name;
	point position;
	public Ninja(String string, point point) {
		this.name = string;
		this.position = point;
	}
	
	public void move(String input)
		{
		String w = "w";
		String e = "e";
		String s = "s";
		String q = "q";
		if(input.equals(w))
			{
				position.x += 10;
			}
		if(input.equals(e))
			{
				position.y += 10;
			}
		if(input.equals(s))
			{
				position.x -= 10;
			}
		if(input.equals(q))
			{
				position.y -= 10;
			}
		}
}
